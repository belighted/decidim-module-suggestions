<div class="card">
  <div class="card-divider">
    <h2 class="card-title">
      <%= t ".title" %>
      <%= link_to t("actions.new", scope: "decidim.admin", name: t("models.suggestions_type_scope.name", scope: "decidim.admin")),
                  new_suggestions_type_suggestions_type_scope_path(suggestion_type),
                  class: "button tiny button--title" if allowed_to? :create, :suggestion_type_scope %>
    </h2>
  </div>
  <div class="row column">
    <div class="table-scroll">
      <table class="table-list">
        <thead>
        <tr>
          <th><%= t("models.suggestions_type_scope.fields.scope", scope: "decidim.admin") %></th>
          <th class="text-right">
            <%= t("models.suggestions_type_scope.fields.supports_required", scope: "decidim.admin") %>
          </th>
          <th class="actions"></th>
        </tr>
        </thead>
        <tbody>
        <% suggestion_type.scopes.each do |s| %>
          <tr>
            <td><%= translated_attribute(s.scope_name) %></td>
            <td class="text-right"><%= s.supports_required %></td>
            <td class="table-list__actions">
              <%= icon_link_to "pencil",
                               decidim_admin_suggestions.edit_suggestions_type_suggestions_type_scope_path(suggestion_type, s),
                               t("actions.configure", scope: "decidim.admin"),
                               class: "action-icon--new" if allowed_to? :edit, :suggestion_type_scope, suggestion_type_scope: s %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
